<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>CreateMesh (553 us, 0.01%)</title><rect x="412.2" y="87" width="0.1" height="15.0" fill="rgb(124.751153743105,124.751153743105,151.564739434149)" rx="2" ry="2" />
<text  x="415.17" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (4,355 us, 0.09%)</title><rect x="410.8" y="119" width="1.1" height="15.0" fill="rgb(143.944926811129,143.944926811129,155.279663253767)" rx="2" ry="2" />
<text  x="413.84" y="129.5" ></text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (735 us, 0.02%)</title><rect x="1189.8" y="103" width="0.2" height="15.0" fill="rgb(145.526946705345,154.766245442126,154.766245442126)" rx="2" ry="2" />
<text  x="1192.82" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (10,763 us, 0.23%)</title><rect x="1186.8" y="103" width="2.7" height="15.0" fill="rgb(126.511159330523,163.126090341543,126.511159330523)" rx="2" ry="2" />
<text  x="1189.76" y="113.5" ></text>
</g>
<g >
<title>CreateExtMesh (1,148,395 us, 24.48%)</title><rect x="123.1" y="71" width="288.8" height="15.0" fill="rgb(124.237703154182,124.237703154182,151.465361900809)" rx="2" ry="2" />
<text  x="126.07" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (1,143,500 us, 24.38%)</title><rect x="123.2" y="87" width="287.6" height="15.0" fill="rgb(152.831313637367,152.831313637367,132.951750704165)" rx="2" ry="2" />
<text  x="126.21" y="97.5" >firedrake.extrusion_utils.make_extr..</text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (879 us, 0.02%)</title><rect x="1189.2" y="167" width="0.3" height="15.0" fill="rgb(150.479242246096,138.728058255621,150.479242246096)" rx="2" ry="2" />
<text  x="1192.24" y="177.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (2,124 us, 0.05%)</title><rect x="1189.5" y="87" width="0.5" height="15.0" fill="rgb(138.703285848435,138.703285848435,154.265152099697)" rx="2" ry="2" />
<text  x="1192.47" y="97.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (10,763 us, 0.23%)</title><rect x="1186.8" y="87" width="2.7" height="15.0" fill="rgb(141.005045454503,160.766620507406,141.005045454503)" rx="2" ry="2" />
<text  x="1189.76" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (4,355 us, 0.09%)</title><rect x="410.8" y="135" width="1.1" height="15.0" fill="rgb(118.83026294198,147.844882984958,147.844882984958)" rx="2" ry="2" />
<text  x="413.84" y="145.5" ></text>
</g>
<g >
<title>Mesh: numbering (1,158 us, 0.02%)</title><rect x="412.3" y="87" width="0.3" height="15.0" fill="rgb(109.216464242517,145.352416655467,145.352416655467)" rx="2" ry="2" />
<text  x="415.30" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (10,763 us, 0.23%)</title><rect x="1186.8" y="135" width="2.7" height="15.0" fill="rgb(151.443810648439,151.443810648439,126.22769775782)" rx="2" ry="2" />
<text  x="1189.76" y="145.5" ></text>
</g>
<g >
<title>all (4,691,149 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(111.427305728195,145.925597781384,145.925597781384)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (4,355 us, 0.09%)</title><rect x="410.8" y="87" width="1.1" height="15.0" fill="rgb(144.480127079177,123.35532564039,144.480127079177)" rx="2" ry="2" />
<text  x="413.84" y="97.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (4,355 us, 0.09%)</title><rect x="410.8" y="103" width="1.1" height="15.0" fill="rgb(140.952029673029,153.580155841156,153.580155841156)" rx="2" ry="2" />
<text  x="413.84" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (10,763 us, 0.23%)</title><rect x="1186.8" y="71" width="2.7" height="15.0" fill="rgb(153.215642937959,121.31992919102,121.31992919102)" rx="2" ry="2" />
<text  x="1189.76" y="81.5" ></text>
</g>
<g >
<title>PetscBarrier (1,176 us, 0.03%)</title><rect x="412.6" y="71" width="0.3" height="15.0" fill="rgb(149.394637843692,149.394637843692,116.297091088661)" rx="2" ry="2" />
<text  x="415.60" y="81.5" ></text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (1,122,664 us, 23.93%)</title><rect x="128.5" y="119" width="282.3" height="15.0" fill="rgb(155.097800745693,135.749805716977,135.749805716977)" rx="2" ry="2" />
<text  x="131.45" y="129.5" >Parloop_Cells_wrap_pyop2_kernel_un..</text>
</g>
<g >
<title>CreateMesh (554 us, 0.01%)</title><rect x="1189.7" y="119" width="0.1" height="15.0" fill="rgb(119.568401350347,119.568401350347,150.561626067809)" rx="2" ry="2" />
<text  x="1192.68" y="129.5" ></text>
</g>
<g >
<title>CreateMesh (2,616 us, 0.06%)</title><rect x="411.9" y="71" width="0.7" height="15.0" fill="rgb(106.54181990571,144.658990345925,144.658990345925)" rx="2" ry="2" />
<text  x="414.94" y="81.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (1,122,664 us, 23.93%)</title><rect x="128.5" y="135" width="282.3" height="15.0" fill="rgb(154.537083127814,131.450970646577,131.450970646577)" rx="2" ry="2" />
<text  x="131.45" y="145.5" >pyop2.global_kernel.GlobalKernel.c..</text>
</g>
<g >
<title>firedrake (4,691,149 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(142.760367242113,142.760367242113,155.050393659764)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (2,124 us, 0.05%)</title><rect x="1189.5" y="71" width="0.5" height="15.0" fill="rgb(127.361327642557,162.987690848886,127.361327642557)" rx="2" ry="2" />
<text  x="1192.47" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (4,355 us, 0.09%)</title><rect x="410.8" y="151" width="1.1" height="15.0" fill="rgb(115.96325539011,115.96325539011,149.863855881957)" rx="2" ry="2" />
<text  x="413.84" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (10,763 us, 0.23%)</title><rect x="1186.8" y="119" width="2.7" height="15.0" fill="rgb(130.398335229631,150.844012837312,150.844012837312)" rx="2" ry="2" />
<text  x="1189.76" y="129.5" ></text>
</g>
<g >
<title>firedrake.__init__ (3,076,547 us, 65.58%)</title><rect x="412.9" y="71" width="773.9" height="15.0" fill="rgb(156.583256387208,147.138298968596,147.138298968596)" rx="2" ry="2" />
<text  x="415.89" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (3,097 us, 0.07%)</title><rect x="1188.7" y="151" width="0.8" height="15.0" fill="rgb(155.550451708698,155.550451708698,146.129112126767)" rx="2" ry="2" />
<text  x="1191.69" y="161.5" ></text>
</g>
<g >
<title>CreateMesh (554 us, 0.01%)</title><rect x="1189.7" y="103" width="0.1" height="15.0" fill="rgb(153.976901603131,127.156245624006,127.156245624006)" rx="2" ry="2" />
<text  x="1192.68" y="113.5" ></text>
</g>
<g >
<title>ParLoopExecute (1,122,664 us, 23.93%)</title><rect x="128.5" y="103" width="282.3" height="15.0" fill="rgb(143.512815485356,154.244063273981,154.244063273981)" rx="2" ry="2" />
<text  x="131.45" y="113.5" >ParLoopExecute</text>
</g>
</g>
</svg>
