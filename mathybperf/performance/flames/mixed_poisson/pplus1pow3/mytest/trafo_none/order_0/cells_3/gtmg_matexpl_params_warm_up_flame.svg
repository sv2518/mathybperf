<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>all (4,838,616 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(146.730598251297,155.078303250336,155.078303250336)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (7,253 us, 0.15%)</title><rect x="1187.4" y="135" width="1.8" height="15.0" fill="rgb(117.849725932576,147.590669686224,147.590669686224)" rx="2" ry="2" />
<text  x="1190.39" y="145.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (3,990 us, 0.08%)</title><rect x="483.2" y="87" width="1.0" height="15.0" fill="rgb(143.86313346552,160.301350366078,143.86313346552)" rx="2" ry="2" />
<text  x="486.20" y="97.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (3,004 us, 0.06%)</title><rect x="1188.4" y="151" width="0.8" height="15.0" fill="rgb(131.924946050996,151.239800828036,151.239800828036)" rx="2" ry="2" />
<text  x="1191.43" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (3,990 us, 0.08%)</title><rect x="483.2" y="151" width="1.0" height="15.0" fill="rgb(125.252427015209,125.252427015209,151.66176006746)" rx="2" ry="2" />
<text  x="486.20" y="161.5" ></text>
</g>
<g >
<title>Mesh: numbering (1,066 us, 0.02%)</title><rect x="484.5" y="87" width="0.3" height="15.0" fill="rgb(122.983383630126,148.921617978181,148.921617978181)" rx="2" ry="2" />
<text  x="487.54" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (1,177 us, 0.02%)</title><rect x="1189.5" y="103" width="0.2" height="15.0" fill="rgb(155.924908879971,142.090968079778,142.090968079778)" rx="2" ry="2" />
<text  x="1192.46" y="113.5" ></text>
</g>
<g >
<title>CreateMesh (2,556 us, 0.05%)</title><rect x="484.2" y="71" width="0.6" height="15.0" fill="rgb(139.143435702089,161.069673257799,139.143435702089)" rx="2" ry="2" />
<text  x="487.17" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (7,253 us, 0.15%)</title><rect x="1187.4" y="71" width="1.8" height="15.0" fill="rgb(116.263507639185,147.179427906455,147.179427906455)" rx="2" ry="2" />
<text  x="1190.39" y="81.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (1,435,464 us, 29.67%)</title><rect x="133.1" y="135" width="350.1" height="15.0" fill="rgb(121.58648689773,148.559459566078,148.559459566078)" rx="2" ry="2" />
<text  x="136.13" y="145.5" >pyop2.global_kernel.GlobalKernel.compile</text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (1,435,464 us, 29.67%)</title><rect x="133.1" y="119" width="350.1" height="15.0" fill="rgb(153.2186493993,121.342978727969,121.342978727969)" rx="2" ry="2" />
<text  x="136.13" y="129.5" >Parloop_Cells_wrap_pyop2_kernel_uniform_ext..</text>
</g>
<g >
<title>firedrake.__init__ (2,879,612 us, 59.51%)</title><rect x="485.1" y="71" width="702.3" height="15.0" fill="rgb(154.619894465622,132.085857569769,132.085857569769)" rx="2" ry="2" />
<text  x="488.14" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>PetscBarrier (1,405 us, 0.03%)</title><rect x="484.8" y="71" width="0.3" height="15.0" fill="rgb(138.255631663227,161.214199496684,138.255631663227)" rx="2" ry="2" />
<text  x="487.80" y="81.5" ></text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (1,453,083 us, 30.03%)</title><rect x="128.8" y="87" width="354.4" height="15.0" fill="rgb(129.896859594738,129.896859594738,152.560682502207)" rx="2" ry="2" />
<text  x="131.83" y="97.5" >firedrake.extrusion_utils.make_extruded_coords</text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (7,253 us, 0.15%)</title><rect x="1187.4" y="103" width="1.8" height="15.0" fill="rgb(149.197131938803,159.433025033218,149.197131938803)" rx="2" ry="2" />
<text  x="1190.39" y="113.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (3,434 us, 0.07%)</title><rect x="1189.2" y="87" width="0.8" height="15.0" fill="rgb(148.004613525766,148.004613525766,109.560819394096)" rx="2" ry="2" />
<text  x="1192.16" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (637 us, 0.01%)</title><rect x="1189.6" y="119" width="0.1" height="15.0" fill="rgb(128.911111710787,150.458436369463,150.458436369463)" rx="2" ry="2" />
<text  x="1192.59" y="129.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (3,434 us, 0.07%)</title><rect x="1189.2" y="71" width="0.8" height="15.0" fill="rgb(150.296582863417,150.296582863417,120.66805541502)" rx="2" ry="2" />
<text  x="1192.16" y="81.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (3,990 us, 0.08%)</title><rect x="483.2" y="103" width="1.0" height="15.0" fill="rgb(118.945450651017,118.945450651017,150.441054964713)" rx="2" ry="2" />
<text  x="486.20" y="113.5" ></text>
</g>
<g >
<title>CreateMesh (649 us, 0.01%)</title><rect x="484.4" y="87" width="0.1" height="15.0" fill="rgb(154.240802819589,129.179488283512,129.179488283512)" rx="2" ry="2" />
<text  x="487.38" y="97.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (7,253 us, 0.15%)</title><rect x="1187.4" y="87" width="1.8" height="15.0" fill="rgb(109.386917410758,109.386917410758,148.59101627305)" rx="2" ry="2" />
<text  x="1190.39" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (7,253 us, 0.15%)</title><rect x="1187.4" y="119" width="1.8" height="15.0" fill="rgb(147.434982000889,159.719886651018,147.434982000889)" rx="2" ry="2" />
<text  x="1190.39" y="129.5" ></text>
</g>
<g >
<title>firedrake (4,838,616 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(146.0454432213,154.900670464782,154.900670464782)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>ParLoopExecute (1,435,464 us, 29.67%)</title><rect x="133.1" y="103" width="350.1" height="15.0" fill="rgb(143.533083566246,143.533083566246,155.199951657983)" rx="2" ry="2" />
<text  x="136.13" y="113.5" >ParLoopExecute</text>
</g>
<g >
<title>CreateExtMesh (1,457,586 us, 30.12%)</title><rect x="128.7" y="71" width="355.5" height="15.0" fill="rgb(125.303976621504,149.523253198168,149.523253198168)" rx="2" ry="2" />
<text  x="131.71" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (3,990 us, 0.08%)</title><rect x="483.2" y="119" width="1.0" height="15.0" fill="rgb(153.681023254868,153.681023254868,137.069574235129)" rx="2" ry="2" />
<text  x="486.20" y="129.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (3,990 us, 0.08%)</title><rect x="483.2" y="135" width="1.0" height="15.0" fill="rgb(149.014110374785,149.014110374785,114.45299643165)" rx="2" ry="2" />
<text  x="486.20" y="145.5" ></text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (1,053 us, 0.02%)</title><rect x="1189.7" y="103" width="0.3" height="15.0" fill="rgb(154.106498635882,154.106498635882,139.131493389274)" rx="2" ry="2" />
<text  x="1192.74" y="113.5" ></text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (874 us, 0.02%)</title><rect x="1188.9" y="167" width="0.3" height="15.0" fill="rgb(144.669281497983,123.840033838581,144.669281497983)" rx="2" ry="2" />
<text  x="1191.95" y="177.5" ></text>
</g>
</g>
</svg>
