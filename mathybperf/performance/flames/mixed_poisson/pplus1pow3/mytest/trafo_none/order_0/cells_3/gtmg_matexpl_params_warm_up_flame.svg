<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (7,284 us, 0.14%)</title><rect x="1187.6" y="135" width="1.7" height="15.0" fill="rgb(146.734423367922,159.833931079641,146.734423367922)" rx="2" ry="2" />
<text  x="1190.64" y="145.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (4,485 us, 0.09%)</title><rect x="451.2" y="119" width="1.1" height="15.0" fill="rgb(113.117050054401,113.117050054401,149.312977429884)" rx="2" ry="2" />
<text  x="454.24" y="129.5" ></text>
</g>
<g >
<title>PetscBarrier (1,342 us, 0.03%)</title><rect x="453.0" y="71" width="0.3" height="15.0" fill="rgb(133.69505335199,161.956619221769,133.69505335199)" rx="2" ry="2" />
<text  x="455.97" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (4,485 us, 0.09%)</title><rect x="451.2" y="87" width="1.1" height="15.0" fill="rgb(136.654255271471,161.474888676737,136.654255271471)" rx="2" ry="2" />
<text  x="454.24" y="97.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (1,333,067 us, 26.41%)</title><rect x="139.7" y="135" width="311.5" height="15.0" fill="rgb(155.239367060663,136.835147465085,136.835147465085)" rx="2" ry="2" />
<text  x="142.66" y="145.5" >pyop2.global_kernel.GlobalKernel.compile</text>
</g>
<g >
<title>CreateMesh (721 us, 0.01%)</title><rect x="452.5" y="87" width="0.2" height="15.0" fill="rgb(155.303227395288,155.303227395288,144.931025069471)" rx="2" ry="2" />
<text  x="455.52" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (672 us, 0.01%)</title><rect x="1189.6" y="119" width="0.2" height="15.0" fill="rgb(143.16893740505,160.414359027085,143.16893740505)" rx="2" ry="2" />
<text  x="1192.64" y="129.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (4,485 us, 0.09%)</title><rect x="451.2" y="151" width="1.1" height="15.0" fill="rgb(154.142441117152,154.142441117152,139.305676183121)" rx="2" ry="2" />
<text  x="454.24" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (4,485 us, 0.09%)</title><rect x="451.2" y="135" width="1.1" height="15.0" fill="rgb(128.816559286713,162.750792674256,128.816559286713)" rx="2" ry="2" />
<text  x="454.24" y="145.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (7,284 us, 0.14%)</title><rect x="1187.6" y="87" width="1.7" height="15.0" fill="rgb(150.810583287339,159.170370162526,150.810583287339)" rx="2" ry="2" />
<text  x="1190.64" y="97.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (2,997 us, 0.06%)</title><rect x="1188.6" y="151" width="0.7" height="15.0" fill="rgb(112.45467443931,146.191952632414,146.191952632414)" rx="2" ry="2" />
<text  x="1191.64" y="161.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (2,813 us, 0.06%)</title><rect x="1189.3" y="87" width="0.7" height="15.0" fill="rgb(118.455754626905,118.455754626905,150.346275089078)" rx="2" ry="2" />
<text  x="1192.34" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (7,284 us, 0.14%)</title><rect x="1187.6" y="103" width="1.7" height="15.0" fill="rgb(126.796729852261,149.910263295031,149.910263295031)" rx="2" ry="2" />
<text  x="1190.64" y="113.5" ></text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (1,350,919 us, 26.76%)</title><rect x="135.5" y="87" width="315.7" height="15.0" fill="rgb(149.167469744035,149.167469744035,115.196199528784)" rx="2" ry="2" />
<text  x="138.48" y="97.5" >firedrake.extrusion_utils.make_extruded..</text>
</g>
<g >
<title>Mesh: numbering (1,227 us, 0.02%)</title><rect x="452.7" y="87" width="0.3" height="15.0" fill="rgb(145.721441157592,159.998835160392,145.721441157592)" rx="2" ry="2" />
<text  x="455.69" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (2,926 us, 0.06%)</title><rect x="452.3" y="71" width="0.7" height="15.0" fill="rgb(155.543873019458,139.169693149177,139.169693149177)" rx="2" ry="2" />
<text  x="455.29" y="81.5" ></text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (876 us, 0.02%)</title><rect x="1189.1" y="167" width="0.2" height="15.0" fill="rgb(128.338728990896,128.338728990896,152.259108836948)" rx="2" ry="2" />
<text  x="1192.14" y="177.5" ></text>
</g>
<g >
<title>CreateMesh (672 us, 0.01%)</title><rect x="1189.6" y="103" width="0.2" height="15.0" fill="rgb(115.949537416936,147.098028219206,147.098028219206)" rx="2" ry="2" />
<text  x="1192.64" y="113.5" ></text>
</g>
<g >
<title>ParLoopExecute (1,333,067 us, 26.41%)</title><rect x="139.7" y="103" width="311.5" height="15.0" fill="rgb(133.90503632996,161.922435946286,133.90503632996)" rx="2" ry="2" />
<text  x="142.66" y="113.5" >ParLoopExecute</text>
</g>
<g >
<title>CreateExtMesh (1,355,943 us, 26.86%)</title><rect x="135.4" y="71" width="316.9" height="15.0" fill="rgb(153.513889481192,153.513889481192,136.259618255006)" rx="2" ry="2" />
<text  x="138.36" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>CreateFunctionSpace (4,485 us, 0.09%)</title><rect x="451.2" y="103" width="1.1" height="15.0" fill="rgb(120.121005232175,148.179519875008,148.179519875008)" rx="2" ry="2" />
<text  x="454.24" y="113.5" ></text>
</g>
<g >
<title>firedrake.__init__ (3,141,832 us, 62.23%)</title><rect x="453.3" y="71" width="734.3" height="15.0" fill="rgb(152.547823837268,152.547823837268,131.577915519069)" rx="2" ry="2" />
<text  x="456.29" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (2,813 us, 0.06%)</title><rect x="1189.3" y="71" width="0.7" height="15.0" fill="rgb(142.696861751288,118.785708237676,142.696861751288)" rx="2" ry="2" />
<text  x="1192.34" y="81.5" ></text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (878 us, 0.02%)</title><rect x="1189.8" y="103" width="0.2" height="15.0" fill="rgb(154.421909740855,154.421909740855,140.66002412876)" rx="2" ry="2" />
<text  x="1192.79" y="113.5" ></text>
</g>
<g >
<title>all (5,048,465 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(118.943863559477,118.943863559477,150.440747785705)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake (5,048,465 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(126.040341463736,149.714162601709,149.714162601709)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (1,333,067 us, 26.41%)</title><rect x="139.7" y="119" width="311.5" height="15.0" fill="rgb(156.331208279807,145.205930145187,145.205930145187)" rx="2" ry="2" />
<text  x="142.66" y="129.5" >Parloop_Cells_wrap_pyop2_kernel_unifor..</text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (7,284 us, 0.14%)</title><rect x="1187.6" y="71" width="1.7" height="15.0" fill="rgb(138.743231556814,153.007504477693,153.007504477693)" rx="2" ry="2" />
<text  x="1190.64" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (7,284 us, 0.14%)</title><rect x="1187.6" y="119" width="1.7" height="15.0" fill="rgb(141.799691540541,153.799920029029,153.799920029029)" rx="2" ry="2" />
<text  x="1190.64" y="129.5" ></text>
</g>
</g>
</svg>
