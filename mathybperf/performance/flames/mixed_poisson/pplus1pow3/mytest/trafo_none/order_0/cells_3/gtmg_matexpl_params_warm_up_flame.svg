<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (1,090,464 us, 23.56%)</title><rect x="145.1" y="119" width="277.9" height="15.0" fill="rgb(128.22093986495,128.22093986495,152.236310941603)" rx="2" ry="2" />
<text  x="148.06" y="129.5" >Parloop_Cells_wrap_pyop2_kernel_un..</text>
</g>
<g >
<title>firedrake.__init__ (2,991,555 us, 64.62%)</title><rect x="425.0" y="71" width="762.5" height="15.0" fill="rgb(149.363674328151,159.405913481464,149.363674328151)" rx="2" ry="2" />
<text  x="427.98" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (6,776 us, 0.15%)</title><rect x="1187.5" y="71" width="1.7" height="15.0" fill="rgb(121.75607875311,148.60342782488,148.60342782488)" rx="2" ry="2" />
<text  x="1190.51" y="81.5" ></text>
</g>
<g >
<title>Mesh: numbering (967 us, 0.02%)</title><rect x="424.4" y="87" width="0.2" height="15.0" fill="rgb(153.43607407589,153.43607407589,135.882512829315)" rx="2" ry="2" />
<text  x="427.39" y="97.5" ></text>
</g>
<g >
<title>all (4,629,396 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(152.956565829812,119.333671361889,119.333671361889)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (6,776 us, 0.15%)</title><rect x="1187.5" y="103" width="1.7" height="15.0" fill="rgb(145.08568070938,160.10233104731,145.08568070938)" rx="2" ry="2" />
<text  x="1190.51" y="113.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (3,993 us, 0.09%)</title><rect x="423.0" y="103" width="1.0" height="15.0" fill="rgb(131.050340282618,162.387153907481,131.050340282618)" rx="2" ry="2" />
<text  x="426.01" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (6,776 us, 0.15%)</title><rect x="1187.5" y="119" width="1.7" height="15.0" fill="rgb(133.038473593954,162.063504298659,133.038473593954)" rx="2" ry="2" />
<text  x="1190.51" y="129.5" ></text>
</g>
<g >
<title>CreateMesh (2,397 us, 0.05%)</title><rect x="424.0" y="71" width="0.6" height="15.0" fill="rgb(127.108241704703,163.028890885281,127.108241704703)" rx="2" ry="2" />
<text  x="427.03" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (6,776 us, 0.15%)</title><rect x="1187.5" y="135" width="1.7" height="15.0" fill="rgb(145.442921348549,154.744461090364,154.744461090364)" rx="2" ry="2" />
<text  x="1190.51" y="145.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (2,935 us, 0.06%)</title><rect x="1188.5" y="151" width="0.7" height="15.0" fill="rgb(135.926627259417,135.926627259417,153.727734308274)" rx="2" ry="2" />
<text  x="1191.49" y="161.5" ></text>
</g>
<g >
<title>ParLoopExecute (1,090,464 us, 23.56%)</title><rect x="145.1" y="103" width="277.9" height="15.0" fill="rgb(120.450412924205,120.450412924205,150.73233798533)" rx="2" ry="2" />
<text  x="148.06" y="113.5" >ParLoopExecute</text>
</g>
<g >
<title>CreateExtMesh (1,111,872 us, 24.02%)</title><rect x="140.6" y="71" width="283.4" height="15.0" fill="rgb(125.951390758133,163.217215457978,125.951390758133)" rx="2" ry="2" />
<text  x="143.62" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (1,107,339 us, 23.92%)</title><rect x="140.8" y="87" width="282.2" height="15.0" fill="rgb(146.853022626743,159.814624223553,146.853022626743)" rx="2" ry="2" />
<text  x="143.76" y="97.5" >firedrake.extrusion_utils.make_ext..</text>
</g>
<g >
<title>PetscBarrier (1,353 us, 0.03%)</title><rect x="424.6" y="71" width="0.4" height="15.0" fill="rgb(140.41984387962,112.950849941526,140.41984387962)" rx="2" ry="2" />
<text  x="427.64" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (3,993 us, 0.09%)</title><rect x="423.0" y="151" width="1.0" height="15.0" fill="rgb(145.331657715094,125.537372894928,145.331657715094)" rx="2" ry="2" />
<text  x="426.01" y="161.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (6,776 us, 0.15%)</title><rect x="1187.5" y="87" width="1.7" height="15.0" fill="rgb(141.652246631043,141.652246631043,154.835918702782)" rx="2" ry="2" />
<text  x="1190.51" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (637 us, 0.01%)</title><rect x="424.2" y="87" width="0.2" height="15.0" fill="rgb(143.759451635884,160.318228803461,143.759451635884)" rx="2" ry="2" />
<text  x="427.23" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (3,993 us, 0.09%)</title><rect x="423.0" y="135" width="1.0" height="15.0" fill="rgb(128.801377837029,128.801377837029,152.348653774909)" rx="2" ry="2" />
<text  x="426.01" y="145.5" ></text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (880 us, 0.02%)</title><rect x="1189.8" y="103" width="0.2" height="15.0" fill="rgb(153.572343422034,124.054632902261,124.054632902261)" rx="2" ry="2" />
<text  x="1192.78" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (3,993 us, 0.09%)</title><rect x="423.0" y="119" width="1.0" height="15.0" fill="rgb(155.607914949744,155.607914949744,146.407587833375)" rx="2" ry="2" />
<text  x="426.01" y="129.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (3,993 us, 0.09%)</title><rect x="423.0" y="87" width="1.0" height="15.0" fill="rgb(113.355687576517,113.355687576517,149.35916533739)" rx="2" ry="2" />
<text  x="426.01" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (1,146 us, 0.02%)</title><rect x="1189.5" y="103" width="0.3" height="15.0" fill="rgb(141.670580070227,116.155861429958,141.670580070227)" rx="2" ry="2" />
<text  x="1192.48" y="113.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (3,001 us, 0.06%)</title><rect x="1189.2" y="87" width="0.8" height="15.0" fill="rgb(139.936144941864,139.936144941864,154.503769988748)" rx="2" ry="2" />
<text  x="1192.24" y="97.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (1,090,464 us, 23.56%)</title><rect x="145.1" y="135" width="277.9" height="15.0" fill="rgb(140.178081325187,160.90124257497,140.178081325187)" rx="2" ry="2" />
<text  x="148.06" y="145.5" >pyop2.global_kernel.GlobalKernel.c..</text>
</g>
<g >
<title>firedrake (4,629,396 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(129.936828664323,129.936828664323,152.568418451159)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (849 us, 0.02%)</title><rect x="1189.0" y="167" width="0.2" height="15.0" fill="rgb(137.858868888468,137.858868888468,154.101716559058)" rx="2" ry="2" />
<text  x="1192.02" y="177.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (3,001 us, 0.06%)</title><rect x="1189.2" y="71" width="0.8" height="15.0" fill="rgb(152.158391555758,113.214335260808,113.214335260808)" rx="2" ry="2" />
<text  x="1192.24" y="81.5" ></text>
</g>
<g >
<title>CreateMesh (654 us, 0.01%)</title><rect x="1189.6" y="119" width="0.2" height="15.0" fill="rgb(154.577475629144,131.760646490107,131.760646490107)" rx="2" ry="2" />
<text  x="1192.61" y="129.5" ></text>
</g>
</g>
</svg>
