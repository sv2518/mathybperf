<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>ParLoopExecute (1,354,664 us, 27.80%)</title><rect x="130.9" y="103" width="328.0" height="15.0" fill="rgb(137.602156308348,161.320579205618,137.602156308348)" rx="2" ry="2" />
<text  x="133.88" y="113.5" >ParLoopExecute</text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (5,493 us, 0.11%)</title><rect x="458.9" y="135" width="1.4" height="15.0" fill="rgb(152.777379631089,144.617035304665,152.777379631089)" rx="2" ry="2" />
<text  x="461.95" y="145.5" ></text>
</g>
<g >
<title>Mesh: numbering (1,068 us, 0.02%)</title><rect x="460.6" y="87" width="0.3" height="15.0" fill="rgb(126.162006238606,126.162006238606,151.837807659085)" rx="2" ry="2" />
<text  x="463.63" y="97.5" ></text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (1,373,348 us, 28.19%)</title><rect x="126.4" y="87" width="332.5" height="15.0" fill="rgb(156.402891904685,145.755504602582,145.755504602582)" rx="2" ry="2" />
<text  x="129.36" y="97.5" >firedrake.extrusion_utils.make_extruded_c..</text>
</g>
<g >
<title>CreateMesh (2,533 us, 0.05%)</title><rect x="460.3" y="71" width="0.6" height="15.0" fill="rgb(149.351016116814,149.351016116814,116.085693489178)" rx="2" ry="2" />
<text  x="463.28" y="81.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (2,428 us, 0.05%)</title><rect x="1189.4" y="87" width="0.6" height="15.0" fill="rgb(148.072966995734,159.616028628601,148.072966995734)" rx="2" ry="2" />
<text  x="1192.41" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (616 us, 0.01%)</title><rect x="460.5" y="87" width="0.1" height="15.0" fill="rgb(125.681860749764,125.681860749764,151.744876274148)" rx="2" ry="2" />
<text  x="463.48" y="97.5" ></text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (1,139 us, 0.02%)</title><rect x="1189.1" y="167" width="0.3" height="15.0" fill="rgb(153.837912394459,126.090661690854,126.090661690854)" rx="2" ry="2" />
<text  x="1192.14" y="177.5" ></text>
</g>
<g >
<title>CreateExtMesh (1,379,651 us, 28.31%)</title><rect x="126.2" y="71" width="334.1" height="15.0" fill="rgb(134.09026201129,161.892282928395,134.09026201129)" rx="2" ry="2" />
<text  x="129.16" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (5,493 us, 0.11%)</title><rect x="458.9" y="151" width="1.4" height="15.0" fill="rgb(115.718909891582,147.038235897818,147.038235897818)" rx="2" ry="2" />
<text  x="461.95" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (7,894 us, 0.16%)</title><rect x="1187.5" y="71" width="1.9" height="15.0" fill="rgb(123.830531880969,163.562471554261,123.830531880969)" rx="2" ry="2" />
<text  x="1190.50" y="81.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (1,354,664 us, 27.80%)</title><rect x="130.9" y="135" width="328.0" height="15.0" fill="rgb(153.068794045034,120.194087678597,120.194087678597)" rx="2" ry="2" />
<text  x="133.88" y="145.5" >pyop2.global_kernel.GlobalKernel.compile</text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (5,493 us, 0.11%)</title><rect x="458.9" y="119" width="1.4" height="15.0" fill="rgb(114.463269723343,114.463269723343,149.573536075486)" rx="2" ry="2" />
<text  x="461.95" y="129.5" ></text>
</g>
<g >
<title>firedrake (4,872,555 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(141.839622725353,160.630759091222,141.839622725353)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (7,894 us, 0.16%)</title><rect x="1187.5" y="135" width="1.9" height="15.0" fill="rgb(138.097772694398,152.84016329114,152.84016329114)" rx="2" ry="2" />
<text  x="1190.50" y="145.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (7,894 us, 0.16%)</title><rect x="1187.5" y="87" width="1.9" height="15.0" fill="rgb(148.149512229302,159.603567776625,148.149512229302)" rx="2" ry="2" />
<text  x="1190.50" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (585 us, 0.01%)</title><rect x="1189.7" y="119" width="0.1" height="15.0" fill="rgb(153.120992204822,153.120992204822,134.355577607981)" rx="2" ry="2" />
<text  x="1192.65" y="129.5" ></text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (1,354,664 us, 27.80%)</title><rect x="130.9" y="119" width="328.0" height="15.0" fill="rgb(155.72774545041,140.57938178648,140.57938178648)" rx="2" ry="2" />
<text  x="133.88" y="129.5" >Parloop_Cells_wrap_pyop2_kernel_uniform_..</text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (852 us, 0.02%)</title><rect x="1189.8" y="103" width="0.2" height="15.0" fill="rgb(145.993137412208,127.232414618784,145.993137412208)" rx="2" ry="2" />
<text  x="1192.79" y="113.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (5,493 us, 0.11%)</title><rect x="458.9" y="103" width="1.4" height="15.0" fill="rgb(123.651739618707,149.094895456702,149.094895456702)" rx="2" ry="2" />
<text  x="461.95" y="113.5" ></text>
</g>
<g >
<title>firedrake.__init__ (2,999,248 us, 61.55%)</title><rect x="461.2" y="71" width="726.3" height="15.0" fill="rgb(154.80409502359,133.498061847524,133.498061847524)" rx="2" ry="2" />
<text  x="464.16" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>all (4,872,555 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(129.558142782434,129.558142782434,152.495124409503)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (3,750 us, 0.08%)</title><rect x="1188.5" y="151" width="0.9" height="15.0" fill="rgb(143.234589185684,154.171930529622,154.171930529622)" rx="2" ry="2" />
<text  x="1191.50" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (7,894 us, 0.16%)</title><rect x="1187.5" y="119" width="1.9" height="15.0" fill="rgb(145.87227531522,154.855775081724,154.855775081724)" rx="2" ry="2" />
<text  x="1190.50" y="129.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (5,493 us, 0.11%)</title><rect x="458.9" y="87" width="1.4" height="15.0" fill="rgb(110.565934733276,145.702279375294,145.702279375294)" rx="2" ry="2" />
<text  x="461.95" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (7,894 us, 0.16%)</title><rect x="1187.5" y="103" width="1.9" height="15.0" fill="rgb(154.767345026638,133.21631187089,133.21631187089)" rx="2" ry="2" />
<text  x="1190.50" y="113.5" ></text>
</g>
<g >
<title>PetscBarrier (1,131 us, 0.02%)</title><rect x="460.9" y="71" width="0.3" height="15.0" fill="rgb(150.444705250597,150.444705250597,121.385879291356)" rx="2" ry="2" />
<text  x="463.89" y="81.5" ></text>
</g>
<g >
<title>CreateMesh (585 us, 0.01%)</title><rect x="1189.7" y="103" width="0.1" height="15.0" fill="rgb(121.799285345859,121.799285345859,150.99341006694)" rx="2" ry="2" />
<text  x="1192.65" y="113.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (2,428 us, 0.05%)</title><rect x="1189.4" y="71" width="0.6" height="15.0" fill="rgb(155.49160165996,138.768946059693,138.768946059693)" rx="2" ry="2" />
<text  x="1192.41" y="81.5" ></text>
</g>
</g>
</svg>
