<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (3,857 us, 0.11%)</title><rect x="207.0" y="119" width="1.3" height="15.0" fill="rgb(153.88334474641,153.88334474641,138.050055309523)" rx="2" ry="2" />
<text  x="209.96" y="129.5" ></text>
</g>
<g >
<title>PetscBarrier (1,369 us, 0.04%)</title><rect x="209.1" y="71" width="0.5" height="15.0" fill="rgb(108.139548305371,145.073216227318,145.073216227318)" rx="2" ry="2" />
<text  x="212.14" y="81.5" ></text>
</g>
<g >
<title>ParLoopExecute (72,405 us, 2.14%)</title><rect x="181.7" y="103" width="25.3" height="15.0" fill="rgb(138.618455222912,108.334791508713,138.618455222912)" rx="2" ry="2" />
<text  x="184.73" y="113.5" >P..</text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (1,027 us, 0.03%)</title><rect x="1189.6" y="103" width="0.4" height="15.0" fill="rgb(151.007807017756,151.007807017756,124.11475708605)" rx="2" ry="2" />
<text  x="1192.64" y="113.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (7,069 us, 0.21%)</title><rect x="1186.4" y="87" width="2.5" height="15.0" fill="rgb(152.683894867005,117.243193980371,117.243193980371)" rx="2" ry="2" />
<text  x="1189.40" y="97.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (2,968 us, 0.09%)</title><rect x="1187.8" y="151" width="1.1" height="15.0" fill="rgb(155.200922339999,136.54040460666,136.54040460666)" rx="2" ry="2" />
<text  x="1190.83" y="161.5" ></text>
</g>
<g >
<title>firedrake (3,385,602 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(152.892707177602,152.892707177602,133.249273245301)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>firedrake.__init__ (2,802,533 us, 82.78%)</title><rect x="209.6" y="71" width="976.8" height="15.0" fill="rgb(148.278514388784,148.278514388784,110.888185114877)" rx="2" ry="2" />
<text  x="212.62" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (3,857 us, 0.11%)</title><rect x="207.0" y="135" width="1.3" height="15.0" fill="rgb(150.286062861819,150.286062861819,120.617073868817)" rx="2" ry="2" />
<text  x="209.96" y="145.5" ></text>
</g>
<g >
<title>CreateMesh (662 us, 0.02%)</title><rect x="1189.4" y="119" width="0.2" height="15.0" fill="rgb(142.647058128862,142.647058128862,155.028462863651)" rx="2" ry="2" />
<text  x="1192.41" y="129.5" ></text>
</g>
<g >
<title>all (3,385,602 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(149.505358759131,149.505358759131,116.833661678864)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (3,857 us, 0.11%)</title><rect x="207.0" y="87" width="1.3" height="15.0" fill="rgb(154.2790276764,129.472545519069,129.472545519069)" rx="2" ry="2" />
<text  x="209.96" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (7,069 us, 0.21%)</title><rect x="1186.4" y="119" width="2.5" height="15.0" fill="rgb(127.718400161886,162.929562764344,127.718400161886)" rx="2" ry="2" />
<text  x="1189.40" y="129.5" ></text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (856 us, 0.03%)</title><rect x="1188.6" y="167" width="0.3" height="15.0" fill="rgb(144.314319472418,160.227901481234,144.314319472418)" rx="2" ry="2" />
<text  x="1191.56" y="177.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (3,857 us, 0.11%)</title><rect x="207.0" y="151" width="1.3" height="15.0" fill="rgb(152.715587923508,117.486174080229,117.486174080229)" rx="2" ry="2" />
<text  x="209.96" y="161.5" ></text>
</g>
<g >
<title>CreateMesh (1,150 us, 0.03%)</title><rect x="1189.2" y="103" width="0.4" height="15.0" fill="rgb(146.424448920152,159.884392036254,146.424448920152)" rx="2" ry="2" />
<text  x="1192.24" y="113.5" ></text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (93,622 us, 2.77%)</title><rect x="174.3" y="87" width="32.7" height="15.0" fill="rgb(147.641644515121,147.641644515121,107.801815727123)" rx="2" ry="2" />
<text  x="177.33" y="97.5" >fi..</text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (3,271 us, 0.10%)</title><rect x="1188.9" y="87" width="1.1" height="15.0" fill="rgb(131.536563073455,151.13910894497,151.13910894497)" rx="2" ry="2" />
<text  x="1191.86" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (7,069 us, 0.21%)</title><rect x="1186.4" y="103" width="2.5" height="15.0" fill="rgb(154.420143106984,130.554430486881,130.554430486881)" rx="2" ry="2" />
<text  x="1189.40" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (7,069 us, 0.21%)</title><rect x="1186.4" y="135" width="2.5" height="15.0" fill="rgb(131.138675152508,131.138675152508,152.801033900485)" rx="2" ry="2" />
<text  x="1189.40" y="145.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (72,405 us, 2.14%)</title><rect x="181.7" y="135" width="25.3" height="15.0" fill="rgb(133.280932744724,162.024034204347,133.280932744724)" rx="2" ry="2" />
<text  x="184.73" y="145.5" >p..</text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (72,405 us, 2.14%)</title><rect x="181.7" y="119" width="25.3" height="15.0" fill="rgb(112.840298637136,112.840298637136,149.259412639446)" rx="2" ry="2" />
<text  x="184.73" y="129.5" >P..</text>
</g>
<g >
<title>CreateMesh (2,383 us, 0.07%)</title><rect x="208.3" y="71" width="0.8" height="15.0" fill="rgb(128.40275677976,150.326640646604,150.326640646604)" rx="2" ry="2" />
<text  x="211.31" y="81.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (3,271 us, 0.10%)</title><rect x="1188.9" y="71" width="1.1" height="15.0" fill="rgb(138.391708499012,138.391708499012,154.20484680626)" rx="2" ry="2" />
<text  x="1191.86" y="81.5" ></text>
</g>
<g >
<title>DMPlexInterp (489 us, 0.01%)</title><rect x="1189.8" y="119" width="0.2" height="15.0" fill="rgb(130.343773052798,162.502176479777,130.343773052798)" rx="2" ry="2" />
<text  x="1192.83" y="129.5" ></text>
</g>
<g >
<title>CreateMesh (602 us, 0.02%)</title><rect x="208.6" y="87" width="0.2" height="15.0" fill="rgb(138.382901223997,161.193481196094,138.382901223997)" rx="2" ry="2" />
<text  x="211.58" y="97.5" ></text>
</g>
<g >
<title>Mesh: numbering (995 us, 0.03%)</title><rect x="208.8" y="87" width="0.3" height="15.0" fill="rgb(128.759783881116,150.419203228437,150.419203228437)" rx="2" ry="2" />
<text  x="211.79" y="97.5" ></text>
</g>
<g >
<title>CreateFunctionSpace (3,857 us, 0.11%)</title><rect x="207.0" y="103" width="1.3" height="15.0" fill="rgb(155.115489703031,135.885421056568,135.885421056568)" rx="2" ry="2" />
<text  x="209.96" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (7,069 us, 0.21%)</title><rect x="1186.4" y="71" width="2.5" height="15.0" fill="rgb(137.327185407006,161.365341910487,137.327185407006)" rx="2" ry="2" />
<text  x="1189.40" y="81.5" ></text>
</g>
<g >
<title>CreateExtMesh (98,001 us, 2.89%)</title><rect x="174.2" y="71" width="34.1" height="15.0" fill="rgb(123.74357656415,123.74357656415,151.369724496287)" rx="2" ry="2" />
<text  x="177.15" y="81.5" >Cr..</text>
</g>
</g>
</svg>
