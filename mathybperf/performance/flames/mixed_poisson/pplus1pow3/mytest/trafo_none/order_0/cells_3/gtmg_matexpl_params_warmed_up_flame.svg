<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="219" onload="init(evt)" viewBox="0 0 1200 219" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:13px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:18px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
			if (!document.querySelector('.parent')) {
				clearzoom();
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 13 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (1 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="219.0" fill="url(#background)"  />
<text id="title" x="600.00" y="26" >Firedrake example</text>
<text id="details" x="10.00" y="201" > </text>
<text id="unzoom" x="10.00" y="26" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="26" >Search</text>
<text id="ignorecase" x="1174.00" y="26" >ic</text>
<text id="matched" x="1090.00" y="201" > </text>
<g id="frames">
<g >
<title>ParLoopExecute (922,172 us, 21.35%)</title><rect x="120.8" y="103" width="251.9" height="15.0" fill="rgb(115.528242102699,115.528242102699,149.779659761813)" rx="2" ry="2" />
<text  x="123.76" y="113.5" >ParLoopExecute</text>
</g>
<g >
<title>CreateExtMesh (958,472 us, 22.19%)</title><rect x="111.9" y="71" width="261.9" height="15.0" fill="rgb(147.13968490881,159.767958270659,147.13968490881)" rx="2" ry="2" />
<text  x="114.93" y="81.5" >CreateExtMesh</text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (3,968 us, 0.09%)</title><rect x="372.7" y="87" width="1.1" height="15.0" fill="rgb(147.69076741009,147.69076741009,108.039872833511)" rx="2" ry="2" />
<text  x="375.73" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspace.VectorFunctionSpace (6,237 us, 0.14%)</title><rect x="1187.7" y="71" width="1.8" height="15.0" fill="rgb(156.300418346199,144.969873987527,144.969873987527)" rx="2" ry="2" />
<text  x="1190.75" y="81.5" ></text>
</g>
<g >
<title>firedrake.utility_meshes.SquareMesh (2,002 us, 0.05%)</title><rect x="1189.5" y="71" width="0.5" height="15.0" fill="rgb(117.930870546007,117.930870546007,150.244684621808)" rx="2" ry="2" />
<text  x="1192.45" y="81.5" ></text>
</g>
<g >
<title>firedrake (4,318,620 us, 100.00%)</title><rect x="10.0" y="55" width="1180.0" height="15.0" fill="rgb(140.396634734378,140.396634734378,154.592897045363)" rx="2" ry="2" />
<text  x="13.00" y="65.5" >firedrake</text>
</g>
<g >
<title>CreateFunctionSpace (6,237 us, 0.14%)</title><rect x="1187.7" y="87" width="1.8" height="15.0" fill="rgb(156.213099672472,144.300430822285,144.300430822285)" rx="2" ry="2" />
<text  x="1190.75" y="97.5" ></text>
</g>
<g >
<title>CreateMesh (500 us, 0.01%)</title><rect x="374.0" y="87" width="0.1" height="15.0" fill="rgb(132.849907380656,93.5528876629301,132.849907380656)" rx="2" ry="2" />
<text  x="377.01" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (3,968 us, 0.09%)</title><rect x="372.7" y="135" width="1.1" height="15.0" fill="rgb(152.60559753183,116.642914410699,116.642914410699)" rx="2" ry="2" />
<text  x="375.73" y="145.5" ></text>
</g>
<g >
<title>pyop2.global_kernel.GlobalKernel.compile (922,172 us, 21.35%)</title><rect x="120.8" y="135" width="251.9" height="15.0" fill="rgb(140.91354971065,160.781515163383,140.91354971065)" rx="2" ry="2" />
<text  x="123.76" y="145.5" >pyop2.global_kernel.GlobalKern..</text>
</g>
<g >
<title>CreateFunctionSpace (3,968 us, 0.09%)</title><rect x="372.7" y="103" width="1.1" height="15.0" fill="rgb(129.714362837197,150.666686661495,150.666686661495)" rx="2" ry="2" />
<text  x="375.73" y="113.5" ></text>
</g>
<g >
<title>CreateMesh (518 us, 0.01%)</title><rect x="1189.7" y="119" width="0.1" height="15.0" fill="rgb(153.776933309915,153.776933309915,137.53436911728)" rx="2" ry="2" />
<text  x="1192.67" y="129.5" ></text>
</g>
<g >
<title>CreateMesh (518 us, 0.01%)</title><rect x="1189.7" y="103" width="0.1" height="15.0" fill="rgb(129.357224674525,150.574095285988,150.574095285988)" rx="2" ry="2" />
<text  x="1192.67" y="113.5" ></text>
</g>
<g >
<title>Mesh: numbering (881 us, 0.02%)</title><rect x="374.1" y="87" width="0.3" height="15.0" fill="rgb(154.52381130282,131.349219988288,131.349219988288)" rx="2" ry="2" />
<text  x="377.15" y="97.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (6,237 us, 0.14%)</title><rect x="1187.7" y="103" width="1.8" height="15.0" fill="rgb(151.872740033063,151.872740033063,128.306355544843)" rx="2" ry="2" />
<text  x="1190.75" y="113.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (6,237 us, 0.14%)</title><rect x="1187.7" y="135" width="1.8" height="15.0" fill="rgb(155.022696467144,155.022696467144,143.571529033081)" rx="2" ry="2" />
<text  x="1190.75" y="145.5" ></text>
</g>
<g >
<title>firedrake.mesh._Facets.__init__ (785 us, 0.02%)</title><rect x="1189.2" y="167" width="0.3" height="15.0" fill="rgb(131.394510035174,131.394510035174,152.850550329388)" rx="2" ry="2" />
<text  x="1192.24" y="177.5" ></text>
</g>
<g >
<title>all (4,318,620 us, 100%)</title><rect x="10.0" y="39" width="1180.0" height="15.0" fill="rgb(125.271528330638,163.327890736873,125.271528330638)" rx="2" ry="2" />
<text  x="13.00" y="49.5" ></text>
</g>
<g >
<title>firedrake.mesh.MeshTopology._facets (2,679 us, 0.06%)</title><rect x="1188.7" y="151" width="0.8" height="15.0" fill="rgb(133.569690792823,161.977027080238,133.569690792823)" rx="2" ry="2" />
<text  x="1191.72" y="161.5" ></text>
</g>
<g >
<title>Parloop_Cells_wrap_pyop2_kernel_uniform_extrusion (922,172 us, 21.35%)</title><rect x="120.8" y="119" width="251.9" height="15.0" fill="rgb(109.526236347329,145.4327279419,145.4327279419)" rx="2" ry="2" />
<text  x="123.76" y="129.5" >Parloop_Cells_wrap_pyop2_kerne..</text>
</g>
<g >
<title>firedrake.__init__ (2,975,756 us, 68.91%)</title><rect x="374.7" y="71" width="813.0" height="15.0" fill="rgb(107.973321534236,145.030120397765,145.030120397765)" rx="2" ry="2" />
<text  x="377.67" y="81.5" >firedrake.__init__</text>
</g>
<g >
<title>firedrake.utility_meshes.RectangleMesh (2,002 us, 0.05%)</title><rect x="1189.5" y="87" width="0.5" height="15.0" fill="rgb(153.676320236731,124.851788481604,124.851788481604)" rx="2" ry="2" />
<text  x="1192.45" y="97.5" ></text>
</g>
<g >
<title>firedrake.mesh._from_cell_list (696 us, 0.02%)</title><rect x="1189.8" y="103" width="0.2" height="15.0" fill="rgb(148.158793689996,132.781908830615,148.158793689996)" rx="2" ry="2" />
<text  x="1192.81" y="113.5" ></text>
</g>
<g >
<title>CreateMesh (2,098 us, 0.05%)</title><rect x="373.8" y="71" width="0.6" height="15.0" fill="rgb(152.122188233005,112.93677645304,112.93677645304)" rx="2" ry="2" />
<text  x="376.82" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.FunctionSpaceData.__init__ (3,968 us, 0.09%)</title><rect x="372.7" y="151" width="1.1" height="15.0" fill="rgb(153.817024195109,125.930518829166,125.930518829166)" rx="2" ry="2" />
<text  x="375.73" y="161.5" ></text>
</g>
<g >
<title>firedrake.functionspaceimpl.FunctionSpace.__init__ (3,968 us, 0.09%)</title><rect x="372.7" y="119" width="1.1" height="15.0" fill="rgb(111.055962756225,145.82932367754,145.82932367754)" rx="2" ry="2" />
<text  x="375.73" y="129.5" ></text>
</g>
<g >
<title>PetscBarrier (1,019 us, 0.02%)</title><rect x="374.4" y="71" width="0.3" height="15.0" fill="rgb(152.471591535132,115.615535102682,115.615535102682)" rx="2" ry="2" />
<text  x="377.39" y="81.5" ></text>
</g>
<g >
<title>firedrake.functionspacedata.get_shared_data (6,237 us, 0.14%)</title><rect x="1187.7" y="119" width="1.8" height="15.0" fill="rgb(155.562641279786,139.313583145023,139.313583145023)" rx="2" ry="2" />
<text  x="1190.75" y="129.5" ></text>
</g>
<g >
<title>firedrake.extrusion_utils.make_extruded_coords (953,840 us, 22.09%)</title><rect x="112.1" y="87" width="260.6" height="15.0" fill="rgb(145.570572726451,160.023395137554,145.570572726451)" rx="2" ry="2" />
<text  x="115.11" y="97.5" >firedrake.extrusion_utils.make_..</text>
</g>
</g>
</svg>
